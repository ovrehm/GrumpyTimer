<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/timer_text_view"
        android:layout_width="match_parent"
        android:layout_height="162dp"
        android:gravity="center_horizontal|center_vertical"
        android:text="10"
        android:textSize="60sp"
        android:textStyle="bold"
        android:textColor="@color/bear_darkbrown" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="62dp"
        android:gravity="center_horizontal|center_vertical"
        android:orientation="horizontal">

        <Button
            android:id="@+id/start_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Start" />
    </LinearLayout>
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Set the initial image of the bear with normal eyebrows -->

        <!-- Set the src to a solid black color resource -->
        <ImageView
            android:id="@+id/bear_image_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:adjustViewBounds="false"
            app:srcCompat="@drawable/bear_image_view" />

        <ImageView
            android:id="@+id/right_eyebrow_image_view"
            android:layout_width="30dp"
            android:layout_height="6dp"
            android:layout_alignParentLeft="true"
            android:layout_marginLeft="173dp"
            android:layout_marginTop="130dp"
            android:scaleType="fitXY"
            android:src="@drawable/eyebrow" />

        <ImageView
            android:id="@+id/left_eyebrow_image_view"
            android:layout_width="30dp"
            android:layout_height="6dp"
            android:layout_alignParentLeft="true"
            android:layout_marginLeft="228dp"
            android:layout_marginTop="130dp"
            android:scaleType="fitXY"
            android:src="@drawable/eyebrow" />


    </RelativeLayout>
</LinearLayout>